{
    "name": "server",
    "version": "0.0.0",
    "private": true,
    "scripts": {
        "base": "esbuild source/index.js --bundle --outdir=dist",
        "build": "npm run base -- --platform=node",
        "watch": "npm run build -- --watch",
        "serve": "nodemon dist/index.js",
        "start": "concurrently npm:watch npm:serve"
    },
    "dependencies": {
        "express": "^4.18.2"
    },
    "devDependencies": {
        "@types/express": "^4.17.17"
    }
}
